# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — NEVER commit HuggingFace tokens or credentials
.env
.env.*
!.env.example

# TRIBE v2 feature cache (regenerable internal pickle format, large)
cache/

# Derived audio (extracted from .mp4 by moviepy — regenerable)
stimulus/*.wav
exp2/stimulus/*.wav

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# uv lock is fine to keep (uv.lock)

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — NEVER commit HuggingFace tokens or credentials
.env
.env.*
!.env.example

# TRIBE v2 feature cache (regenerable internal pickle format, large)
cache/
stimulus/*.wav
stimulus/*.tsv

# Stimulus video IS committed (50MB, under GitHub limit)
# stimulus/*.mp4 intentionally tracked

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# uv lock is fine to keep (uv.lock)
